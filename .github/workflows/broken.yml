name: site-check
on:  push 
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: get the code
        uses: actions/checkout@v4 
      - name: Read the readme
        run: cat README.md
      - name: Show the Python version
        run: python3 --version
